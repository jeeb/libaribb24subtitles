#include "../common.h"void dumpPacket(uint8_t *data){				uint8_t i=0;	uint8_t width = 16;	char* tmp = malloc(7);	memset(tmp,0,7);	while (i < 188){		sprintf(tmp,"0x%04x ",i);		fwrite(tmp,7,1,stderr);		int j = i;		while (j < i+width && j < 188){			/*char *bitstr = bitstrd_ui(data[j],8);			fwrite(bitstr,strlen(bitstr),1,stderr);			fwrite(" ",2,1,stderr);			free(bitstr);*/			sprintf(tmp,"%02x ",data[j]);			fwrite(tmp,4,1,stderr);			j++;		}		i = j;		fwrite("\n",2,1,stderr);	} 	free(tmp);}